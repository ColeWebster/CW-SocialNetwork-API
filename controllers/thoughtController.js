const { Thought, User } = require("../models");

//Create CRUD commands to grab information
const thoughtController = {
    // Get all the thoughts
    // Get a single thought by an ID
    // Create a thought
    // Update thoughts
    // Delete thoughts
    // Add reaction to thoughts
    // Remove reaction to thoughts
}
//Export
module.export - thoughtController;